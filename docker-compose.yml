services:
  myagency-bot:
    build: .
    container_name: myagency-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/myagency.db
      - TZ=Europe/Moscow
